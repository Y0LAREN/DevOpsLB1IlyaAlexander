# DevOpsLB1IlyaAlexander

Лабораторная работа №1

Студент
ФИО: Иванов Илья Андреевич
Группа: ПИ-432Б

---

Ход выполнения работы
1. Подготовка среды: установка Docker на Mac, настройка терминала.
2. Создание Dockerfile и docker-compose.yml для развёртывания приложения.
3. Сборка и запуск контейнеров через Docker Compose.
4. Проверка работы приложения и подключение к базе данных.
5. Решение возникающих ошибок (например, корректировка флагов команд для Mac).
6. Подготовка к загрузке проекта на GitHub: инициализация git, создание ветки main, добавление всех файлов и коммитов.

---

Заключение
В ходе работы была изучена установка и использование Docker на Mac, создание и запуск контейнеров, работа с базой данных внутри контейнера. Проект был успешно собран и подготовлен для размещения на GitHub. Полученные навыки позволят в дальнейшем эффективно развёртывать приложения в контейнерах.

---

Контрольные вопросы

1. Что такое Docker и зачем он нужен?
Docker — это платформа для контейнеризации приложений, которая позволяет запускать их в изолированной среде на любом компьютере без конфликта зависимостей.

2. Что такое Dockerfile?
Dockerfile — это текстовый файл, содержащий инструкции для сборки Docker-образа, включая установку зависимостей, копирование файлов и запуск команд внутри контейнера.

3. Что такое docker-compose.yml?
Это конфигурационный файл для Docker Compose, который позволяет описывать и запускать многоконтейнерные приложения с настройкой сетей, томов и зависимостей между контейнерами.

4. Как запускать контейнеры на Mac через Docker?
Используется терминал Mac с командой `docker-compose up -d` (для запуска в фоне) после настройки Docker Desktop для Mac.

5. Как подготовить проект для GitHub?
Через команды `git init`, `git add .`, `git commit -m "initial commit"`, `git branch -M main`, `git remote add origin <URL>` и `git push -u origin main`.


<img width="1151" height="369" alt="image" src="https://github.com/user-attachments/assets/c82530a9-ff28-4c71-8dd9-d1ab754c0abf" />

<img width="641" height="163" alt="image" src="https://github.com/user-attachments/assets/2b513cf7-7d1f-487f-b4dc-b79a9f9bfdc9" />

<img width="576" height="378" alt="image" src="https://github.com/user-attachments/assets/b0bfd584-d1cf-4fff-a2b1-8fc9fbd1c315" />

<img width="346" height="284" alt="image" src="https://github.com/user-attachments/assets/1a575dba-8883-4fa2-902f-ab8c384db2ec" />


